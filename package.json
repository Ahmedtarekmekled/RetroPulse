{
  "name": "portfolio",
  "version": "1.0.0",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "cd backend && npm install && npm start",
    "db:migrate": "echo 'No migrations to run'",
    "postinstall": "npm run build"
  }
}
